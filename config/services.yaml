google: ''
openai:
  required: ["base-url", "api-key", "model"]
  rpm: 60
glm:
  type: openai
  max-concurrency: 100
  extra-body:
    thinking:
      type: disabled
siliconflow:
  type: openai
  rpm: 300
  extra-body:
    enable_thinking: false
