google: ''
openai:
  required: ["base-url", "api-key", "model"]
  rpm: 60
glm:
  type: openai
  max-concurrency: 100
  request-args:
    thinking:
      type: disabled
siliconflow:
  type: openai
  rpm: 300
  request-args:
    enable_thinking: false
